--- c:/Users/joogando/Desktop/ADEMI_BD/historias/QA02_BUG_AUTOINDEXADO_REPRESTAMOS/comparacion_diff/InsertUrlReporte_ACTUAL_SIN_FIX.sql	2026-04-30 14:42:54.108288600 -0400
+++ c:/Users/joogando/Desktop/ADEMI_BD/historias/QA02_BUG_AUTOINDEXADO_REPRESTAMOS/comparacion_diff/InsertUrlReporte_CON_FIX.sql	2026-04-30 14:43:25.551624700 -0400
@@ -1,7 +1,12 @@
 -- ============================================================================
--- ACTUAL (SIN FIX) - InsertUrlReporte
--- Procedimiento tal cual fue enviado por el usuario el 2026-04-30.
--- Estado: NO tiene el fix de Represtamos aplicado.
+-- CON FIX - InsertUrlReporte
+-- Mismo procedure que ACTUAL pero con el fix de Represtamos aplicado.
+-- Cambios en 4 campos del SELECT interno (subquery):
+--   1. tipo_identificacion  - agrega 'Represtamo' + discriminador parte(3) IS NOT NULL
+--   2. identificacion       - agrega 'Represtamo' + discriminador parte(3) IS NOT NULL
+--   3. f_num_prestamo       - agrega 'Represtamo' + parte 3 si existe, sino parte 1
+--   4. codigo_agencia       - agrega 'Represtamo' + reemplaza url_reporte por parte(3)
+-- Sin cambios fuera del SELECT interno.
 -- Paquete: PA.PKG_TIPO_DOCUMENTO_PKM
 -- ============================================================================
 
@@ -107,8 +112,11 @@
             SELECT r.CODIGO_REPORTE,
                    r.CODIGO_REFERENCIA,
                    r.ORIGEN_PKM,
-                       case when r.ORIGEN_PKM in ('Normal') then
-                            nvl(ia.pkg_api_pkm.obtienepartereferencia(r.codigo_referencia, ':', 1), '1')
+                       -- FIX 1: tipo_identificacion
+                       case when r.ORIGEN_PKM in ('Normal','Represtamo') then
+                            case when ia.pkg_api_pkm.obtienepartereferencia(r.codigo_referencia, ':', 3) is not null then
+                                nvl(ia.pkg_api_pkm.obtienepartereferencia(r.codigo_referencia, ':', 1), '1')
+                            else null end
                             WHEN R.ORIGEN_PKM IN ('Onboarding', 'TarjetaPC', 'Tarjeta') THEN
                               CASE WHEN r.URL_REPORTE is not null THEN
                                 (SELECT st.COD_TIPO_ID
@@ -119,10 +127,13 @@
                                     nvl(ia.pkg_api_pkm.obtienepartereferencia(r.codigo_referencia, ':', 1), '1')
                               END
                        end as tipo_identificacion,
-                       case when r.ORIGEN_PKM in ('Normal') then
-                                pa.formatear_identificacion(ia.pkg_api_pkm.obtienepartereferencia(r.codigo_referencia, ':', 2),
-                                                            (select mascara from tipos_id where cod_tipo_id = ia.pkg_api_pkm.obtienepartereferencia(r.codigo_referencia, ':', 1)),
-                                                            'ESPA')
+                       -- FIX 2: identificacion
+                       case when r.ORIGEN_PKM in ('Normal','Represtamo') then
+                                case when ia.pkg_api_pkm.obtienepartereferencia(r.codigo_referencia, ':', 3) is not null then
+                                    pa.formatear_identificacion(ia.pkg_api_pkm.obtienepartereferencia(r.codigo_referencia, ':', 2),
+                                                                (select mascara from tipos_id where cod_tipo_id = ia.pkg_api_pkm.obtienepartereferencia(r.codigo_referencia, ':', 1)),
+                                                                'ESPA')
+                                else null end
                             WHEN R.ORIGEN_PKM IN ('Onboarding', 'TarjetaPC', 'Tarjeta') THEN
                                 case when r.url_reporte is not null then
                                     (select st.num_id
@@ -135,8 +146,10 @@
                                                                 'ESPA')
                                 END
                        end as identificacion,
-                       case when r.ORIGEN_PKM in ('Normal') then
-                                ia.pkg_api_pkm.obtienepartereferencia(r.codigo_referencia, ':', 3)
+                       -- FIX 3: f_num_prestamo
+                       case when r.ORIGEN_PKM in ('Normal','Represtamo') then
+                                ia.pkg_api_pkm.obtienepartereferencia(r.codigo_referencia, ':',
+                                    case when ia.pkg_api_pkm.obtienepartereferencia(r.codigo_referencia, ':', 3) is not null then 3 else 1 end)
                             WHEN R.ORIGEN_PKM IN ('Onboarding', 'TarjetaPC', 'Tarjeta') THEN
                                 ia.pkg_api_pkm.obtienepartereferencia(r.codigo_referencia, ':', CASE WHEN r.url_reporte is not null THEN 1 ELSE 3 END)
                        end as f_num_prestamo,
@@ -144,11 +157,13 @@
                        substr(replace(r.nombre_archivo, ':', '_'), 1, instr(replace(r.nombre_archivo, ':', '_'), '_') - 1) as tipo_archivo,
                        ia.pkg_api_pkm.obtienepartereferencia(r.codigo_referencia, ':', 6) as id_tempfud,
                        replace(r.nombre_archivo, ':', '_') as nombre_archivo,
-                       case when r.origen_pkm in ('Normal') then
+                       -- FIX 4: codigo_agencia
+                       case when r.origen_pkm in ('Normal','Represtamo') then
                                 (select cr.codigo_agencia
                                    from pr.pr_creditos cr
                                   where cr.codigo_empresa = '1'
-                                    and cr.no_credito = ia.pkg_api_pkm.obtienepartereferencia(r.codigo_referencia, ':', case when r.url_reporte is null then 3 else 1 end)
+                                    and cr.no_credito = ia.pkg_api_pkm.obtienepartereferencia(r.codigo_referencia, ':',
+                                        case when ia.pkg_api_pkm.obtienepartereferencia(r.codigo_referencia, ':', 3) is not null then 3 else 1 end)
                                 )
                             WHEN R.ORIGEN_PKM IN ('Onboarding', 'TarjetaPC', 'Tarjeta') THEN
                                 (SELECT to_number(st.oficina)
