# oracle-compare-environments

Alias en inglés de `comparar_entornos`.

Usa exactamente la misma base operativa:
- `docs/instrucciones_ai/BASE_OPERATIVA.md`
- `docs/instrucciones_ai/referencias/`

**Argumento:** `$ARGUMENTS`
